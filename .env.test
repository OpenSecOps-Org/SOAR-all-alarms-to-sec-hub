# Test Environment Configuration for SOAR All Alarms to Security Hub
# Copy this file to .env.test and customize as needed

# AWS Configuration (for moto mocking)
AWS_ACCESS_KEY_ID=testing
AWS_SECRET_ACCESS_KEY=testing
AWS_SECURITY_TOKEN=testing
AWS_SESSION_TOKEN=testing
AWS_DEFAULT_REGION=us-east-1

# Test Account Configuration
TEST_ACCOUNT_ID=123456789012

# Application Environment Variables (for function testing)
ALARM_TYPE=soar-cloudwatch-alarms-test
COMPANY_NAME=Test Company SOAR

# LocalStack Configuration (optional - for integration testing)
LOCALSTACK_ENDPOINT=http://localhost:4566
SERVICES=securityhub,sts,lambda,states

# Test Control Flags
RUN_INTEGRATION_TESTS=false
RUN_REAL_AWS_TESTS=false
SKIP_SLOW_TESTS=true